package maznin.monitoring;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Точка входа Spring Boot. Описание системы и обзор пакетов — в
 * документации пакета {@link maznin.monitoring}.
 */
@SpringBootApplication
public class PatientMonitoringApplication {
    public static void main(String[] args) {
        SpringApplication.run(PatientMonitoringApplication.class, args);
    }
}
